# Gateway container image pins
#
# This file is the single source of truth for which container images run on the gateway.
# Quadlet unit files reference these variables in their Image= lines.
#
# Notes:
# - Use explicit tags or (preferably) digests for reproducibility.
# - OTA of the container stack updates this file (without editing unit files).
#

# Core app pod
# NOTE: These images are built/pushed by this repo's GitHub Actions workflows (see Vibrant.Platform/.github/workflows/build-gateway-*.yaml).
# Tags:
# - latest: most recent build for the branch
# - <run_id>: immutable CI run tag
VBT_POSTGRES_IMAGE=harbor-dev.vibrantbt.net/gateway/db:2026.02.05-7d23daa4
VBT_EVENTSRV_IMAGE=harbor-dev.vibrantbt.net/gateway/realtime:latest
VBT_POSTGREST_IMAGE=harbor-dev.vibrantbt.net/gateway/api:2026.01.25-99c845c6
VBT_GATEWAYOS_IMAGE=harbor-dev.vibrantbt.net/gateway/gatewayos:2026.02.07-a9f52c68
VBT_ZONEMANAGER_IMAGE=harbor-dev.vibrantbt.net/gateway/zonemanager:2026.02.05-7d23daa4
VBT_COAPBROKER_IMAGE=harbor-dev.vibrantbt.net/gateway/coap-broker:latest

# System-level services
# Home Assistant is upstream for now (not built in our CI workflows).
VBT_HOMEASSISTANT_IMAGE=harbor-dev.vibrantbt.net/gateway/homeassistant:2026.1.0
